/*
 * LibreriaOperaciones.h
 *
 *  Created on: 31 mar. 2022
 *      Author: fserrano
 *
 *      Acá van solo los includes de todas las bibliotecas que vaya a usar
 *      (entre #ifndef y #endif)
 *      y las definiciones de las funciones (no el desarrollo)
 */

#ifndef LIBRERIAOPERACIONES_H_
#define LIBRERIAOPERACIONES_H_

#include <stdio.h>
#include <stdlib.h>

float sumar(float n1, float n2);
float restar(float n1, float n2);
float multiplicar(float n1, float n2);
float dividirFloat(float n1, float n2);

#endif /* LIBRERIAOPERACIONES_H_ */

